<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.borqs.ivi_collect"
    android:versionCode="1"
    android:versionName="1.0" >

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Receiver -->
        <receiver android:name=".MessageReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        
        <receiver android:name=".collector.ShutDownReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
            </intent-filter>
        </receiver>

        <!-- Services -->
        <service android:name=".ProcessorService" >
            <intent-filter>
                <action android:name="com.borqs.ivi_collect.ACTION_MSG_BOOT" />
            </intent-filter>
        </service>
        
        <service android:name=".SendAllService" >
            <intent-filter>
                <action android:name="com.borqs.ivi_collect.SEND_ALL_REPORT" />
            </intent-filter>
        </service>
        
        <service android:name=".SendLittleService" >
            <intent-filter>
                <action android:name="com.borqs.ivi_collect.SEND_LITTLE_REPORT" />
            </intent-filter>
        </service>
        
        <service android:name=".collector.DataCollector" >
            <intent-filter>
                <action android:name="com.borqs.ivi_collect.DATA_REPORT" />
            </intent-filter>
        </service>
    </application>

</manifest>